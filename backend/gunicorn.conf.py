"""Gunicorn production configuration for IAMS backend."""
import multiprocessing

# ---------------------------------------------------------------------------
# Server socket
# ---------------------------------------------------------------------------
bind = '0.0.0.0:5000'
backlog = 2048

# ---------------------------------------------------------------------------
# Worker processes
# ---------------------------------------------------------------------------
workers = min(multiprocessing.cpu_count(), 4)
threads = 2
worker_class = 'gthread'
worker_connections = 1000
timeout = 30
keepalive = 2

# ---------------------------------------------------------------------------
# Worker lifecycle — recycle to avoid memory leaks
# ---------------------------------------------------------------------------
max_requests = 5000
max_requests_jitter = 500

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
accesslog = '-'
errorlog = '-'
loglevel = 'info'
access_log_format = (
    '%(h)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s '
    '"%(f)s" "%(a)s" %(D)sμs'
)

# ---------------------------------------------------------------------------
# Graceful shutdown
# ---------------------------------------------------------------------------
graceful_timeout = 30
